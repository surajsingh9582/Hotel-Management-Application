spring:
  application:
    name: User-Service
  datasource:
    url: jdbc:mysql://localhost:3306/micro_service?createDatabaseIfNotExist=true
    username: root
    password: Thisisme@123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
eureka:
  instance:
    prefer-ip-address: true
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8167/eureka/